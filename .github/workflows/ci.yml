# The build loop. Until now nothing ran the tests on a pull request — the suite
# only ever ran on a release tag, which is the one moment it is too late to hear
# about a failure. Every PR and every push to main now builds, tests, renders the
# deck in a real browser, and leaves a screenshot behind as an artifact.
name: CI

on:
  pull_request:
  # a merge queue tests each PR against `main + everything ahead of it in the
  # queue` on a gh-readonly-queue/* ref, then merges in order. Without this
  # trigger the queue's required checks never run and nothing ever lands.
  merge_group:
  push:
    branches: [main]

permissions:
  contents: read

# a second push supersedes the first: nobody needs the results of a commit that
# has already been replaced
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: build · test · render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 20 # the floor in package.json engines — test what we promise
          cache: npm

      # node-pty (optional dep) is CLI-recording only — skip its native build
      - run: npm ci --omit=optional

      # the render harnesses drive a real headless browser; without one they exit
      # 1 rather than pretending to pass
      - uses: browser-actions/setup-chrome@v2
        id: chrome
        with:
          chrome-version: stable

      - name: unit tests
        run: npm test

      - name: build, render, contrast, palette (npm run verify)
        run: npm run verify
        env:
          CHROME: ${{ steps.chrome.outputs.chrome-path }}

      # The deck has to still LOOK like a deck. A render harness proves the DOM;
      # this proves the pixels, and it is the same tool the ready-to-dev loop uses
      # to show a reviewer what a change actually did.
      - name: screenshot the demo deck
        run: |
          mkdir -p .shots
          node tools/shot.mjs demo/showcase.html -o .shots/showcase.png --wait 2500
          node tools/shot.mjs demo/showcase.html -o .shots/showcase-slide-3.png --slide 3 --wait 2500
        env:
          CHROME: ${{ steps.chrome.outputs.chrome-path }}

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: shots
          path: .shots/
          # .shots is a DOTTED directory and upload-artifact skips hidden files by
          # default — without this it uploads nothing, cheerfully, and the step
          # still goes green
          include-hidden-files: true
          retention-days: 14

  # Windows is where the bug that started the soak came from — a raw ENOENT on
  # any path with a space in it, on every profile named "Firstname Lastname" —
  # and until now nothing here ever ran on it. `decklight` is published to npm
  # for every platform Node runs on; testing one of them is a claim about the
  # other two.
  #
  # UNIT TESTS ONLY, deliberately. `npm run verify` drives a headless browser
  # through fifteen render harnesses, and a rendering difference on another OS is
  # a different investigation from "does the code run there at all" — one worth
  # having, but not worth blocking every PR on before the first question is
  # answered. This job answers the first question, including the platform module
  # test/soak.mjs branches on (test/soak-platform.test.mjs), which is otherwise
  # only ever checked by pretending.
  windows:
    name: test · windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 20 # the floor in package.json engines — test what we promise
          cache: npm

      # node-pty needs a C++ toolchain and is CLI-recording only; the suite skips
      # what it gates (test/helpers.mjs optionalDepSkip) rather than failing
      - run: npm ci --omit=optional

      # --test-timeout turns a HANG into a named failure. Both runs here died to
      # the job timeout after 29 minutes of SILENCE with no clue which file was
      # stuck: node --test reports a file only when it finishes, so a file that
      # never finishes reports nothing at all. 120s is far above the slowest
      # legitimate test (~12s) and far below this job's own limit.
      - name: unit tests
        run: npm test -- --test-timeout=120000

  # `npm test` answers "does the code run there". This asks the other question
  # (#309): does it LOOK right there. They are different investigations, and the
  # second one is where platforms actually differ — font stacks, subpixel
  # metrics, DPI, and Chrome's own per-platform defaults. The overflow guardrail
  # (SPEC PRESENTING) asserts about text metrics directly, so a font present on
  # Ubuntu and absent elsewhere is a real way for this to fail.
  #
  # fail-fast is OFF on purpose: the point is to learn about BOTH platforms in
  # one run, and a Windows failure that cancels the macOS job answers half a
  # question. Same reasoning as test/verify.mjs itself, which runs all seventeen
  # harnesses rather than stopping at the first.
  verify-elsewhere:
    name: verify · ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # 45, not 30: the first macOS attempt died to a 30-minute limit and a
    # cancelled job's logs cannot be fetched afterwards, so it reported nothing
    # at all. test/verify.mjs now kills any single harness at 10 minutes and
    # names it, which is the failure this limit should never be the one to catch.
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 20 # the floor in package.json engines — test what we promise
          cache: npm

      - run: npm ci --omit=optional

      - uses: browser-actions/setup-chrome@v2
        id: chrome
        with:
          chrome-version: stable

      # Before seventeen harnesses drive it: does this machine's Chrome answer at
      # ALL? On macOS every harness was killed at its 600s limit having produced
      # nothing, which is a browser that never returns rather than a rendering
      # difference — and telling those apart is what this whole job is for. Each
      # variant is bounded at 60s and reports what it did, so the answer arrives
      # in one minute instead of one 30-minute round per hypothesis.
      - name: probe the browser before driving it (#309)
        timeout-minutes: 5
        shell: bash
        run: |
          node - <<'PROBE' 2>&1 | tee "probe-${{ matrix.os }}.log"
          const { execFileSync } = require('child_process');
          const { mkdtempSync } = require('fs');
          const { tmpdir } = require('os');
          const { join } = require('path');
          const chrome = process.env.CHROME;
          const fresh = () => mkdtempSync(join(tmpdir(), 'chrome-probe-'));
          try {
            console.log('version:', execFileSync(chrome, ['--version'], { timeout: 30000, encoding: 'utf8' }).trim());
          } catch (e) { console.log('version: FAILED —', e.message); }
          const probe = (label, args) => {
            const at = Date.now();
            const secs = () => ((Date.now() - at) / 1000).toFixed(1);
            try {
              const out = execFileSync(chrome, [...args, '--dump-dom', 'about:blank'],
                { timeout: 60000, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] });
              console.log(`${label}: OK in ${secs()}s — ${out.length} bytes`);
            } catch (e) {
              const why = String(e.stderr || e.message).trim().split('\n').slice(-3).join(' | ');
              console.log(`${label}: ${e.code ?? 'FAILED'} after ${secs()}s — ${why}`);
            }
          };
          // What the harnesses pass today.
          probe('as we call it', ['--headless', '--disable-gpu', '--no-sandbox']);
          // A profile of its own: without one Chrome uses the real user profile,
          // which on a macOS runner can block on a keychain prompt nobody answers.
          probe('own profile   ', ['--headless', '--disable-gpu', '--no-sandbox',
            `--user-data-dir=${fresh()}`, '--no-first-run', '--no-default-browser-check']);
          // The pre-132 headless implementation, in case the full-browser one is
          // what stalls here.
          probe('old headless  ', ['--headless=old', '--disable-gpu', '--no-sandbox',
            `--user-data-dir=${fresh()}`, '--no-first-run']);
          PROBE
        env:
          CHROME: ${{ steps.chrome.outputs.chrome-path }}

      # THE TIMEOUT IS ON THE STEP, NOT THE JOB, and the output is teed to a
      # file. Three macOS attempts reported nothing at all: a job cancelled by
      # its own `timeout-minutes` discards its logs, and the `if: always()`
      # steps below never ran either, so there was no way to tell a slow
      # harness from a stuck one. A step that times out FAILS instead — the
      # later steps run, and the log is uploaded with whatever it got to.
      #
      # pipefail because a pipeline reports the LAST command's status, and
      # `tee` always succeeds: without it a failing verify would upload its own
      # failure and go green.
      - name: build, render, contrast, palette (npm run verify)
        timeout-minutes: 30
        shell: bash
        run: |
          set -o pipefail
          npm run verify 2>&1 | tee "verify-${{ matrix.os }}.log"
        env:
          CHROME: ${{ steps.chrome.outputs.chrome-path }}
          # A hung harness names itself in three minutes rather than ten, so a
          # broken browser costs one round and not seventeen. Three minutes is
          # ~8x the slowest harness on a healthy machine.
          VERIFY_TIMEOUT_MS: 180000

      # The per-harness timings, which are the whole point on a machine nobody
      # can attach to — and the one artifact that survives the step failing.
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: verify-log-${{ matrix.os }}
          # The probe's answer rides in the ARTIFACT, not only in the job log:
          # a job that gets cancelled — by its own cap, or by the next push —
          # discards its log, and this one has now eaten three rounds' evidence.
          path: |
            verify-*.log
            probe-*.log
          retention-days: 14

      # Beside the Ubuntu job's shots, so a difference is VISIBLE rather than
      # described. This runs even when verify failed — a render that went wrong
      # is exactly the one worth looking at.
      # Bounded like everything else here, and for the reason this job keeps
      # relearning: tools/shot.mjs hands Chrome to execFile with no timeout of
      # its own, so on a machine where the browser never returns this step is
      # the one that runs out the JOB's clock — and a cancelled job uploads
      # nothing and keeps no log. No step here may be the step that cancels it.
      - name: screenshot the demo deck
        if: always()
        timeout-minutes: 5
        continue-on-error: true
        run: |
          mkdir -p .shots
          node tools/shot.mjs demo/showcase.html -o .shots/showcase.png --wait 2500
          node tools/shot.mjs demo/showcase.html -o .shots/showcase-slide-3.png --slide 3 --wait 2500
        env:
          CHROME: ${{ steps.chrome.outputs.chrome-path }}

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: shots-${{ matrix.os }}
          path: .shots/
          include-hidden-files: true
          retention-days: 14
