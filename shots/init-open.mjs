// The deck `decklight init --open` just launched in the player's browser
// (scaffolded by shots/init-open-transcript.gen.mjs, opened via its file://
// URL): advance onto the build slide and reveal two steps — the presentation
// a player is looking at seconds after running init.
press('ArrowRight'); await sleep(300);
press('ArrowRight'); await sleep(300);
press('ArrowRight'); await sleep(400);
