// The charts slide (4): a grouped bar chart and a donut declared as inline
// JSON, sharing a split layout — every color a theme diagram token, so the
// same shot in two themes proves the repaint.
const deck = document.querySelector('.decklight').__decklight;
deck.goto(4, 0);
await sleep(800);
